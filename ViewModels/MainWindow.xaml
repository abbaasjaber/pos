<Window x:Class="NexusPOS.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NexusPOS.Views.Windows"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="768" Width="1366"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Navigation Panel -->
        <Border Grid.Column="0" Background="#FF2D2D30" >
            <StackPanel Margin="10">
                <TextBlock Text="NexusPOS" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,30"/>
                <Button Content="داشبورد" Margin="5" Padding="10" Command="{Binding NavigateToCommand}" CommandParameter="Dashboard" />
                <Button Content="صندوق فروش" Margin="5" Padding="10" Command="{Binding NavigateToCommand}" CommandParameter="POS"/>
                <Button Content="مدیریت کالاها" Margin="5" Padding="10" Command="{Binding NavigateToCommand}" CommandParameter="Products"/>
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1">
            <ContentControl Content="{Binding CurrentView}" Margin="10"/>
        </Grid>

    </Grid>
</Window>