<Window x:Class="NexusPOS.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NexusPOS.ViewModels"
        xmlns:v="clr-namespace:NexusPOS.Views"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="NexusPOS" Height="720" Width="1280"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft"
        FontFamily="Segoe UI">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}"><v:DashboardView/></DataTemplate>
        <DataTemplate DataType="{x:Type vm:POSViewModel}"><v:POSView/></DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProductViewModel}"><v:ProductView/></DataTemplate>
    </Window.Resources>

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Border Background="{StaticResource BackgroundBrush}" CornerRadius="8" ClipToBounds="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/> <!-- Sidebar -->
                <ColumnDefinition Width="*"/>   <!-- Content -->
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}">
                <StackPanel Margin="10">
                    <TextBlock Text="NEXUS POS" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="10,20,10,30" HorizontalAlignment="Center"/>
                    
                    <Button Content="داشبورد" Margin="0,5" Command="{Binding ChangeViewCommand}" CommandParameter="Dashboard"/>
                    <Button Content="صندوق فروش" Margin="0,5" Command="{Binding ChangeViewCommand}" CommandParameter="POS"/>
                    <Button Content="مدیریت کالاها" Margin="0,5" Command="{Binding ChangeViewCommand}" CommandParameter="Products"/>
                    <Button Content="گزارشات" Margin="0,5" Command="{Binding ChangeViewCommand}" CommandParameter="Reports"/>
                    <Button Content="تنظیمات" Margin="0,5" Command="{Binding ChangeViewCommand}" CommandParameter="Settings"/>
                </StackPanel>
            </Border>

            <!-- Content -->
            <ContentControl Grid.Column="1" Content="{Binding CurrentView}" Margin="20"/>
        </Grid>
    </Border>
</Window>