<UserControl x:Class="NexusPOS.Views.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NexusPOS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header and Actions -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="افزودن کالای جدید" Padding="10,5" Command="{Binding OpenAddProductCommand}"/>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="300" Margin="20,0,0,0" VerticalContentAlignment="Center"/>
            <Label Content="جستجو بر اساس نام کالا..."/>
        </StackPanel>

        <!-- Products Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding FilteredProducts}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="کد" Binding="{Binding Code}" Width="120"/>
                <DataGridTextColumn Header="نام کالا" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="بارکد" Binding="{Binding Barcode}" Width="150"/>
                <DataGridTextColumn Header="قیمت خرید" Binding="{Binding BuyPrice, StringFormat=N0}" Width="120"/>
                <DataGridTextColumn Header="قیمت فروش" Binding="{Binding SellPrice, StringFormat=N0}" Width="120"/>
                <DataGridTextColumn Header="موجودی" Binding="{Binding Stock}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>